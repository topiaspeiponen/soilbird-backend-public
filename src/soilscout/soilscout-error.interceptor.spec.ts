import { SoilscoutErrorInterceptor } from './soilscout-error.interceptor';

describe('SoilscoutInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new SoilscoutErrorInterceptor()).toBeDefined();
  });
});
